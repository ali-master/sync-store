generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["metrics", "relationJoins", "views", "typedSql", "driverAdapters", "queryCompiler"]
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
